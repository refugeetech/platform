if(Meteor.isClient) {
    Template.sectionHowItWorks.events({
        'click .button'(event) {
            //event.preventDefault();
            //console.log(event);
            //$('.button').toggleClass('is-active');
         }
     });
}
